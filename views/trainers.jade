extends layout

block content
  h1= title
  p Welcome to #{title}
  - for (var i = 0; i < data.length; i++){
    button
      a(href='/trainers/#{i}') #{data[i]["name"]}
  -}
  form(method="get", action="/trainers/new")
    input(type="submit" value="Create")
