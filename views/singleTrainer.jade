extends layout

block content
  h1= data[0]["name"]


  p ID  
  h3= data[0]["id"]
  p Pokemon
  - for (var i = 0; i < data.length; i++){
    button
      a(href="/pokemon/#{data[i]['pokeNum']}") #{data[i]["pokemon_name"]}
  -}
  //- h3= data[i]["pokemon_name"]
  
  form(method="get" action="/trainers/#{trainerID}/edit")
    input(type="submit" value="Edit Trainer")
  form(method="post" action="/trainers/#{trainerID}/delete")
    input(type="submit" value="Delete Trainer")
